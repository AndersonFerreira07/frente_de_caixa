import React, { FC } from 'react';

export type DialogoConfirmacaoProps = {};

const DialogoConfirmacao: FC<DialogoConfirmacaoProps> = () => {
  /* const elements = React.Children.toArray(children);
  type CountdownHandle = React.ElementRef<typeof any>;
  const componentRef = useRef<CountdownHandle>(null); */
  return <></>;
};

export default DialogoConfirmacao;
